export interface profileInterface {
  id?: string;
  companyName: string;
  street: string;
  postal: string;
  city: string;
  tel: string;
  taxNumber: string;
  taxId?: string;
  firstName: string;
  lastName: string;
  iban: string;
  bic: string;

  defaultText: string;
}
